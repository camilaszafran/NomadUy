// Locale-aware navigation helpers — use in server components for type-safe routing.
// For client locale switching, use useLocale() + usePathname() from 'next/navigation'.
export { createNavigation } from 'next-intl/navigation'
export { routing } from './routing'
